import 'package:flutter/material.dart';

class BotChannels extends StatelessWidget {
  const BotChannels({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('This is BotChannels Page');
  }
}
